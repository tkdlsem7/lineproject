import axios from "axios";

// 대시보드에서 호출할 submit 함수
export const handleDashboardSubmit = async (): Promise<void> => {
  try {
    alert("ASFAFSFAS");  // 메시지 출력
  } catch (error) {
    console.error("대시보드 처리 중 오류 발생:", error);
  }
};
